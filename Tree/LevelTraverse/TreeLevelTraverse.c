#include<stdio.h>

typedef struct *node
{
    int data;
    struct *node left;
    struct *node right;
	struct *node date;

    struct *node Queue;
}node;

node *insert(node *Nnode, int value)
{
	if(Nnode == NULL)
		return Nnode;
	i
}
